<UserControl x:Class="ClientPDS.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"              
             xmlns:local="clr-namespace:ClientPDS"
             mc:Ignorable="d" 
             
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition /> <!-- Data grid row-->
            <RowDefinition Height="25"/> <!-- Notification bar row-->
        </Grid.RowDefinitions>
        
        <DataGrid Name ="Apps" ItemsSource="{Binding Processes}" Grid.Row="0"
                   local:DataGridTextSearch.SearchValue="{Binding ElementName=FocusedProcessPidTxt, Path=Text, 
            UpdateSourceTrigger=PropertyChanged}" >

            <DataGrid.Resources>
                <local:SearchValueConverter x:Key="SearchValueConverter" />
                <Style TargetType="{x:Type DataGridCell}">
                    <Setter Property="local:DataGridTextSearch.IsTextMatch">
                        <Setter.Value>
                            <MultiBinding Converter="{StaticResource SearchValueConverter}">
                                <Binding RelativeSource="{RelativeSource Self}" Path="Content.Text" />
                                <Binding RelativeSource="{RelativeSource Self}" Path="(local:DataGridTextSearch.SearchValue)" />
                            </MultiBinding>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="local:DataGridTextSearch.IsTextMatch" Value="True">
                            <Setter Property="Background" Value="Orange" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="App Name"></DataGridTextColumn>
                <DataGridTextColumn Header="Pid" ></DataGridTextColumn>
                <DataGridTextColumn Header="Description"></DataGridTextColumn>
                <DataGridTextColumn Header="Status"></DataGridTextColumn>
                <DataGridTextColumn Header="Percentage"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
        <Label Name="FocusedProcesLabel"  Content="Pid del processo in focus:" />
        <TextBox Name="FocusedProcessPidTxt" IsReadOnly="True" Text="{Binding Path=FocusPid}" VerticalAlignment="Bottom"
                   />
        </StackPanel>
    </Grid>


</UserControl>
